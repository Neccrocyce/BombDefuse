<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/da_btn_reset">

        <TextView
            android:id="@+id/da_txt_code"
            style="@style/textfield_bigg"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="5dp"
            android:background="@color/colorWhiteGrey"
            android:textAlignment="center"
            android:layout_gravity="center"/>
    </FrameLayout>

    <Button
        android:id="@+id/da_btn_reset"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/textfield_default"
        android:text="@string/bd_txt_reset"
        android:layout_above="@id/da_btn_main_menu"
        android:onClick="gotoDefuseReady"/>

    <Button
        android:id="@+id/da_btn_main_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/bd_txt_main_menu"
        android:layout_alignParentBottom="true"
        android:onClick="gotoMainActivity"/>

</RelativeLayout>